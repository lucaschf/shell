package tsi.too.lucaschfonseca.shell.impl;

import tsi.too.lucaschfonseca.shell.api.Session;

public class ShellSession extends Session {
    public ShellSession(String identifier) {
        super(identifier);
    }
}
